# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'New Design' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for New Design

pod 'Cosmos', '~> 18.0'

  target 'New DesignTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'New DesignUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
